{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit User</h1>

    {{ include('user/_form.html.twig', {'button_label': 'update'}) }}

    <a href="{{ path('ro_user_index') }}" class="btn btn-sm btn-outline-primary">{{ 'back'|trans }}</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
