{% extends 'base.html.twig' %}

{% block body %}
    <main role="main" class="container">
        <div class="row">
            <div class="col-md-12">
                <h3 class="pb-3 mb-4 font-weight-bold border-bottom">
                    {{ 'edit'|trans|capitalize ~ ' ' ~ 'scientific_interest'|trans }}
                </h3>

                <div class="border-bottom pb-3 mb-4">
                    {{ include('scientific_interest/_form.html.twig', {'button_label': 'update'}) }}

                    {% include 'components/_buttons.thml.twig' with {
                        'id': scientific_interest.id,
                        'back': 'ro_scientific_interest_index',
                        'delete': 'ro_scientific_interest_delete'
                    } %}
                </div>
            </div>
        </div>
    </main>
{% endblock %}
