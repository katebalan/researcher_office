{% extends 'base.html.twig' %}

{% block body %}
    <main role="main" class="container">
        <div class="row">
            <div class="col-md-12">
                <h3 class="pb-3 mb-4 font-weight-bold border-bottom">
                    {{ 'edit'|trans|capitalize ~ ' ' ~ 'diploma'|trans }}
                </h3>

                <div class="border-bottom pb-3 mb-4">
                    {{ include('diploma/_form.html.twig', {'button_label': 'update', 'diploma_id': diploma.id}) }}

                    {% include 'components/_buttons.html.twig' with {
                        'id': diploma.id,
                        'back': 'ro_diploma_index',
                        'delete': 'ro_diploma_delete'
                    } %}
                </div>
            </div>
        </div>
    </main>
{% endblock %}
