<form method="post" action="{{ path('individual_plan_delete', {'id': individual_plan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ individual_plan.id) }}">
    <button class="btn">Delete</button>
</form>
